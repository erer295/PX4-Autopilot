# ZD680 北大核心论文初稿：GPT 资料打包与提交说明

生成日期：2026-07-16  
项目根目录：`/home/lst/PX4-Autopilot`  
当前主分支：`rbf-ladrc-work`  
当前主代码提交：`b9671fe69aabd759b856aafa5720a1a53097df12`

## 1. 先说结论

不要把整个 `/home/lst/PX4-Autopilot` 压缩后一次性提交给 GPT。完整工程、构建目录和原始日志体积过大，而且包含旧数据、无关 PX4 文件和不同实验阶段，容易让 GPT 混淆。

建议生成五个独立压缩包：

| 压缩包 | 用途 | 是否首轮提交GPT | 预计内容规模 |
|---|---|---:|---:|
| `ZD680_01_论文初稿核心资料.zip` | 控制代码、仿真模型、任务书、阶段报告 | **必须提交** | 较小 |
| `ZD680_02_实验汇总与统计数据.zip` | n=3、VFB、功率分解的CSV/JSON/摘要 | **必须提交** | 较小 |
| `ZD680_03_VFB原始日志_20260716.zip` | 最新4次VFB完整原始记录 | 先保存，按需提交 | 约198 MB未压缩 |
| `ZD680_04_固定电池n3原始日志.zip` | PID+AS、LADRC+AS、FULL的10次原始记录 | 先保存，按需提交 | 约466 MB未压缩 |
| `ZD680_05_早期完整性验证原始日志.zip` | 早期消融、绳长、质量、转弯、外扰、ramp | 可选审计，不首轮提交 | 约1.2 GB未压缩 |

推荐先把前两个小包和本说明文档交给 GPT。原始 ULog 包主要用于重新作图和审计，不需要一开始全部上传。

## 2. 最紧急的操作

最新 VFB 原始数据当前仍在临时目录：

```text
/tmp/zd680_vfb_z_observation_clean_20260716
```

该目录约198 MB，包含4份完整ULog、关节真值、events、metadata、validity、summary和启动日志。`/tmp` 可能在重启或系统清理后丢失，因此应优先生成第三个压缩包，或至少先复制到桌面永久目录。

不要把下面两个诊断目录当作正式数据提交：

```text
/tmp/zd680_vfb_validation_20260716
/tmp/zd680_vfb_ekf_baro_20260716
```

前者是较早的一轮VFB数据，后者是用于调查Z轴问题的baro诊断数据；最新正式采用的是：

```text
/tmp/zd680_vfb_z_observation_clean_20260716
```

## 3. 建议建立的资料包根目录

建议在桌面建立：

```text
/home/lst/Desktop/ZD680_GPT论文资料包_20260716
```

手动执行：

```bash
ROOT=/home/lst/Desktop/ZD680_GPT论文资料包_20260716
mkdir -p "$ROOT"
```

## 4. 压缩包一：论文初稿核心资料

建议压缩包名称：

```text
ZD680_01_论文初稿核心资料.zip
```

### 4.1 必须包含的主控制代码和报告目录

完整复制：

```text
/home/lst/PX4-Autopilot/src/modules/mc_pos_control
```

该目录已经包含：

- LADRC位置控制；
- EKF–ESO速度融合VFB；
- AS能量抗摆；
- PAS/ACTIVE命令级能量协调；
- 位置恢复权限和总加速度包络；
- 参数定义；
- 单元测试；
- 功率诊断日志映射；
- 2026-07-12至2026-07-16的阶段报告；
- nominal分解CSV；
- VFB汇总CSV/JSON；
- 自动验证脚本。

### 4.2 必须包含的PX4机架参数

文件：

```text
/home/lst/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/22002_gz_zd680_hang
```

### 4.3 必须单独包含的Gazebo模型

这些目录位于嵌套Gazebo仓库中，目前不是主PX4提交的一部分，必须单独复制：

```text
/home/lst/PX4-Autopilot/Tools/simulation/gz/models/zd680
/home/lst/PX4-Autopilot/Tools/simulation/gz/models/zd680_base
/home/lst/PX4-Autopilot/Tools/simulation/gz/models/zd680_hang
/home/lst/PX4-Autopilot/Tools/simulation/gz/models/zd680_load
```

同时复制当前使用的world：

```text
/home/lst/PX4-Autopilot/Tools/simulation/gz/worlds/default.sdf
```

### 4.4 必须单独包含的Gazebo自动测试工具

至少包含：

```text
/home/lst/PX4-Autopilot/Tools/simulation/gz/tools/position_offboard_flight_recorder.py
/home/lst/PX4-Autopilot/Tools/simulation/gz/tools/zd680_validation_suite.py
/home/lst/PX4-Autopilot/Tools/simulation/gz/tools/zd680_validation_report.py
```

建议同时包含该目录下现有的ZD680和吊挂分析脚本：

```text
/home/lst/PX4-Autopilot/Tools/simulation/gz/tools
```

但可以排除与ZD680无关的脚本，只保留文件名中包含下列关键词的文件：

```text
zd680
position_offboard
pid_ladrc
suspended
hang
```

### 4.5 必须包含的任务书

建议复制整个目录：

```text
/home/lst/Desktop/总结文档
```

其中最重要的是：

```text
/home/lst/Desktop/总结文档/CODEX_ZD680_HANG_缺少实验与统一验证任务书.md
/home/lst/Desktop/总结文档/CODEX任务书：ZD680吊挂无人机EKF–ESO融合速度反馈自动验证.docx
/home/lst/Desktop/总结文档/CODEX_ZD680_HANG_新会话交接文档_2026-07-13.md
```

注意：旧交接文档中的HEAD `a52d94d59b`已经过时。资料包首页必须说明当前版本是：

```text
b9671fe69aabd759b856aafa5720a1a53097df12
```

### 4.6 核心资料包生成命令

```bash
ROOT=/home/lst/Desktop/ZD680_GPT论文资料包_20260716
CORE="$ROOT/01_论文初稿核心资料"

mkdir -p "$CORE/code" "$CORE/config" "$CORE/gazebo/models" \
  "$CORE/gazebo/tools" "$CORE/gazebo/worlds" "$CORE/taskbooks"

cp -a /home/lst/PX4-Autopilot/src/modules/mc_pos_control \
  "$CORE/code/"

cp -a /home/lst/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/22002_gz_zd680_hang \
  "$CORE/config/"

cp -a /home/lst/PX4-Autopilot/Tools/simulation/gz/models/zd680 \
  /home/lst/PX4-Autopilot/Tools/simulation/gz/models/zd680_base \
  /home/lst/PX4-Autopilot/Tools/simulation/gz/models/zd680_hang \
  /home/lst/PX4-Autopilot/Tools/simulation/gz/models/zd680_load \
  "$CORE/gazebo/models/"

cp -a /home/lst/PX4-Autopilot/Tools/simulation/gz/tools/position_offboard_flight_recorder.py \
  /home/lst/PX4-Autopilot/Tools/simulation/gz/tools/zd680_validation_suite.py \
  /home/lst/PX4-Autopilot/Tools/simulation/gz/tools/zd680_validation_report.py \
  "$CORE/gazebo/tools/"

cp -a /home/lst/PX4-Autopilot/Tools/simulation/gz/worlds/default.sdf \
  "$CORE/gazebo/worlds/"

cp -a /home/lst/Desktop/总结文档/. "$CORE/taskbooks/"

git -C /home/lst/PX4-Autopilot show -s \
  --format='commit=%H%nauthor=%an%ndate=%aI%nsubject=%s' \
  b9671fe69aabd759b856aafa5720a1a53097df12 \
  > "$CORE/CODE_VERSION.txt"

git -C /home/lst/PX4-Autopilot format-patch -1 --stdout \
  b9671fe69aabd759b856aafa5720a1a53097df12 \
  > "$CORE/CODE_CHANGE_b9671fe69a.patch"

cd "$ROOT"
zip -r ZD680_01_论文初稿核心资料.zip 01_论文初稿核心资料
```

如果不希望压缩包包含Python缓存，可在压缩前删除暂存副本中的`__pycache__`，不要删除项目源目录中的任何文件。

## 5. 压缩包二：实验汇总与统计数据

建议压缩包名称：

```text
ZD680_02_实验汇总与统计数据.zip
```

这个包应当保持较小，优先提交给GPT。它不包含几十MB的ULog，只包含GPT能直接读取的CSV、JSON和Markdown。

### 5.1 固定电池n=3数据

来源目录：

```text
/home/lst/PX4-Autopilot/build/zd680_fixed_battery_value_n3_20260715
```

必须包含：

```text
repeat_all_runs.csv
repeat_n3_analysis.json
repeat_n3_report.md
summaries/
launcher_results.json
```

其中`summaries/`包含每次试验的metadata、validity、events和summary，能够证明样本为什么有效或作废。

### 5.2 VFB汇总数据

持久化来源目录：

```text
/home/lst/PX4-Autopilot/src/modules/mc_pos_control/zd680_vfb_validation_20260716/report
```

必须包含：

```text
validation_runs.csv
validation_comparisons.csv
window_metrics.csv
validation_summary.json
validation_summary.md
```

同时从最新原始目录复制小型摘要文件：

```text
/tmp/zd680_vfb_z_observation_clean_20260716/summaries
/tmp/zd680_vfb_z_observation_clean_20260716/launcher_results.json
```

### 5.3 nominal与功率分解数据

完整复制：

```text
/home/lst/PX4-Autopilot/src/modules/mc_pos_control/zd680_nominal_component_analysis_20260715
```

### 5.4 汇总数据包生成命令

```bash
ROOT=/home/lst/Desktop/ZD680_GPT论文资料包_20260716
DATA="$ROOT/02_实验汇总与统计数据"

mkdir -p "$DATA/fixed_battery_n3" "$DATA/vfb/report" \
  "$DATA/vfb/summaries" "$DATA/nominal_power"

cp -a /home/lst/PX4-Autopilot/build/zd680_fixed_battery_value_n3_20260715/repeat_all_runs.csv \
  /home/lst/PX4-Autopilot/build/zd680_fixed_battery_value_n3_20260715/repeat_n3_analysis.json \
  /home/lst/PX4-Autopilot/build/zd680_fixed_battery_value_n3_20260715/repeat_n3_report.md \
  /home/lst/PX4-Autopilot/build/zd680_fixed_battery_value_n3_20260715/launcher_results.json \
  "$DATA/fixed_battery_n3/"

cp -a /home/lst/PX4-Autopilot/build/zd680_fixed_battery_value_n3_20260715/summaries \
  "$DATA/fixed_battery_n3/"

cp -a /home/lst/PX4-Autopilot/src/modules/mc_pos_control/zd680_vfb_validation_20260716/report/. \
  "$DATA/vfb/report/"

cp -a /tmp/zd680_vfb_z_observation_clean_20260716/summaries/. \
  "$DATA/vfb/summaries/"

cp -a /tmp/zd680_vfb_z_observation_clean_20260716/launcher_results.json \
  "$DATA/vfb/"

cp -a /home/lst/PX4-Autopilot/src/modules/mc_pos_control/zd680_nominal_component_analysis_20260715/. \
  "$DATA/nominal_power/"

cd "$ROOT"
zip -r ZD680_02_实验汇总与统计数据.zip 02_实验汇总与统计数据
```

## 6. 压缩包三：最新VFB原始日志

建议压缩包名称：

```text
ZD680_03_VFB原始日志_20260716.zip
```

唯一正式来源：

```text
/tmp/zd680_vfb_z_observation_clean_20260716
```

直接保存：

```bash
ROOT=/home/lst/Desktop/ZD680_GPT论文资料包_20260716
cd /tmp
zip -r "$ROOT/ZD680_03_VFB原始日志_20260716.zip" \
  zd680_vfb_z_observation_clean_20260716
```

生成后应确认桌面压缩包存在，再考虑关闭或重启系统。

该包应长期保存。首轮写论文时不一定上传给GPT；当需要重新画VFB时间曲线、核对功率峰值或审查Z问题时再提供。

## 7. 压缩包四：固定电池n=3原始日志

建议压缩包名称：

```text
ZD680_04_固定电池n3原始日志.zip
```

来源目录：

```text
/home/lst/PX4-Autopilot/build/zd680_fixed_battery_value_n3_20260715
```

该目录约466 MB，包含：

- `PID_AS`有效3次和一次Z异常判废样本；
- `ABL_LADRC_AS`有效3次；
- `ABL_FULL`有效3次；
- 共10份ULog、joint、events、metadata、validity和summary；
- 启动和记录器日志。

生成命令：

```bash
ROOT=/home/lst/Desktop/ZD680_GPT论文资料包_20260716
cd /home/lst/PX4-Autopilot/build
zip -r "$ROOT/ZD680_04_固定电池n3原始日志.zip" \
  zd680_fixed_battery_value_n3_20260715
```

该包是目前最重要的重复性原始证据，必须本地保存；只有在GPT需要复算或画时间序列时才上传。

## 8. 压缩包五：早期完整性验证原始日志

建议压缩包名称：

```text
ZD680_05_早期完整性验证原始日志.zip
```

来源目录：

```text
/home/lst/PX4-Autopilot/build/zd680_validation_feasibility_20260714
```

该目录约1.2 GB，包含26份ULog，覆盖消融、绳长、质量、转弯、动态外扰和ramp预验证。但其中部分试验受早期模拟电池设置影响，因此只能作为研究过程和趋势审计，不能作为最终主表。

生成命令：

```bash
ROOT=/home/lst/Desktop/ZD680_GPT论文资料包_20260716
cd /home/lst/PX4-Autopilot/build
zip -r "$ROOT/ZD680_05_早期完整性验证原始日志.zip" \
  zd680_validation_feasibility_20260714
```

此包可以最后再做，也可以只在本地保留，不建议首轮上传GPT。

## 9. 不要打包或不要首轮提交的目录

不要直接压缩整个：

```text
/home/lst/PX4-Autopilot
```

特别不要提交：

```text
/home/lst/PX4-Autopilot/build/px4_sitl_default
/home/lst/PX4-Autopilot/build/px4_sitl_test
/home/lst/PX4-Autopilot/.git
/home/lst/PX4-Autopilot/git
```

理由：这些内容体积大、与论文无关，或可能包含未知/内部版本信息。

也不要把以下诊断数据作为正式样本：

```text
/tmp/zd680_vfb_validation_20260716
/tmp/zd680_vfb_ekf_baro_20260716
```

不要把已经不存在的旧`/tmp`路径写成“已随资料提供”。例如：

```text
/tmp/zd680_complex_conditions_o24_20260715
/tmp/zd680_complex_conditions_o24_20260715_retry
/tmp/zd680_o24_full_confirmation_20260715
/tmp/zd680_wcwo_final_confirmation_20260715
/tmp/zd680_nominal_component_diag_20260715
```

这些目录已经不存在，目前只有对应报告和部分派生CSV。

## 10. 给GPT的提交顺序

### 第一轮

提交：

```text
本说明文档
ZD680_01_论文初稿核心资料.zip
ZD680_02_实验汇总与统计数据.zip
目标期刊模板或一篇近期样文
参考文献表、BibTeX或论文PDF
```

先要求GPT完成：

1. 阅读资料并生成证据优先级清单；
2. 提出论文题目和三级提纲；
3. 明确哪些数据可作为n=3结果、哪些只是单次预验证；
4. 列出不能写的过度结论；
5. 生成缺少图表和文献清单；
6. 经人工确认后再写全文。

### 第二轮

如果GPT需要重画VFB曲线或复算功率，再提交：

```text
ZD680_03_VFB原始日志_20260716.zip
```

如果GPT需要复核n=3时间曲线，再提交：

```text
ZD680_04_固定电池n3原始日志.zip
```

### 第三轮或审稿复核

只有需要追溯完整研究过程时才提交：

```text
ZD680_05_早期完整性验证原始日志.zip
```

## 11. 可以直接粘贴给GPT的总说明

```text
我要撰写一篇中文北大核心期刊论文初稿，研究对象是ZD680吊挂无人机。

主代码版本：PX4分支rbf-ladrc-work，提交
b9671fe69aabd759b856aafa5720a1a53097df12。

当前方法包括：
1. X/Y二阶位置LADRC，Z轴保留PX4原生PID；
2. 绳长相关频率调度；
3. 基于吊载能量的AS抗摆；
4. PAS/ACTIVE命令级功率协调；
5. 位置恢复权限和总水平加速度包络；
6. EKF–ESO速度反馈融合VFB，用于削弱v_ekf-z2观察误差造成的激摆功率。

证据优先级必须遵守：
第一优先：固定电池n=3重复消融数据；
第二优先：2026-07-16 VFB四组机理验证；
第三优先：nominal/z3及观察误差功率分解；
第四优先：0.8 m、动态横扰等单次复杂工况；
早期完整性实验只用于趋势和研究过程。

必须遵守以下事实：
1. 当前固定电池n=3结果中，PID+AS优于LADRC+AS和FULL；
2. PAS在LADRC+AS基础上有小幅、同方向、可重复的附加减摆作用；
3. VFB W=0.5显著降低观察误差、速度总项和nominal净注能，但整体判定为DECISION_B，不能写成已经全面改善摆动；
4. 0.8 m和动态横扰结果是单次预验证，不能写成统计结论；
5. Z轴偶发真值误差问题仍保留，本轮只作为观测限制；
6. 所有核心结果来自PX4 SITL+Gazebo，没有实机结果；
7. 不得声称FULL全面优于PID+AS；
8. 不得编造文献、显著性、置信区间、实机结果或不存在的数据。

推荐论文主线：提出一种LADRC—能量抗摆—命令级功率协调框架，分析ESO速度观察误差造成的激摆机理，并通过EKF–ESO速度融合削弱该注能通道；论文重点是结构、机理、功率分解、约束协调和适用边界，而不是声称所有指标全面优于PID+AS。

请先不要直接生成全文。先完成：
1. 资料证据分级；
2. 论文题目候选；
3. 三级提纲；
4. 每一章对应的数据和文件来源；
5. 需要补充的文献和图表；
6. 可能被审稿人质疑的地方；
7. 建议删除或降低权重的过度结论。

得到我的确认后，再生成中文论文初稿。引用资料中不存在的信息时必须标记“待补”，不得自行虚构。
```

## 12. 资料包目前仍缺少的内容

在正式让GPT写全文前，最好再加入：

```text
目标北大核心期刊名称和模板
作者、单位、基金信息
参考文献BibTeX、DOI清单或论文PDF
期望的中文题目方向
论文篇幅和图表数量限制
是否接受全部实验来自SITL
```

当前没有找到可直接使用的`.bib`文献库、论文PDF和正式论文图表。GPT可以先生成初稿结构和图表清单，但不能让它编造文献条目。

## 13. 最终核对清单

压缩完成后检查：

- [ ] `ZD680_01_论文初稿核心资料.zip`存在且可正常解压；
- [ ] `ZD680_02_实验汇总与统计数据.zip`存在且可正常解压；
- [ ] `/tmp/zd680_vfb_z_observation_clean_20260716`已经保存为第三个压缩包；
- [ ] 核心包中有`CODE_VERSION.txt`，提交号为`b9671fe69a...`；
- [ ] 核心包中包含Gazebo的4个ZD680模型目录；
- [ ] 核心包中包含记录器、验证套件和报告脚本；
- [ ] 汇总包中包含固定电池n=3的CSV/JSON；
- [ ] 汇总包中包含VFB的`window_metrics.csv`；
- [ ] 没有把baro诊断数据当作正式结果；
- [ ] 给GPT的说明明确写出PID+AS当前优于FULL；
- [ ] 给GPT的说明明确写出VFB结论是DECISION_B；
- [ ] 已提供真实参考文献，或明确要求引用位置暂用“待补”；
- [ ] 没有把单次预验证写成统计显著性结论。

完成前两个压缩包后，已经足够让GPT开始整理提纲和撰写论文初稿。第三、第四、第五包主要用于保存原始证据和后续绘图，不必全部首轮上传。
