# 阶段1判定与阶段2条件激活记录

记录时间：2026-07-22，阶段2正式闭环运行前。

阶段1按预注册脚本得到：

| 估计器 | 合成中位绝对误差 | 合成P95 | 飞行/FFT中位差 | 低激励高可信误触发 |
|---|---:|---:|---:|---:|
| 当前HESO bank | 0.54% | 6.64% | 5.00% | 0 |
| 校正前预测残差 bank | 0.78% | 6.01% | 11.19% | 0 |
| 滤波RLS | **0.33%** | **1.42%** | 5.82% | 0 |

滤波RLS为合成真值集胜出结构，但飞行/FFT中位差5.82%高于预注册5%门槛，因此 `winner_meets_all_targets=false`。按照预注册条件：

- 不允许RLS进入闭环调度；
- 阶段2主要执行A/B真值频率因果试验；
- RLS只以C影子模式记录，倍率严格为1；
- 原计划D（RLS闭环）不激活，不用闭环偶然结果覆盖估计器门槛失败。

阶段2正式顺序由预注册的条件分支具体化为：

```text
A1 -> B1 -> C1 -> C2 -> B2 -> A2
```

- A：`STD_L06_PID_AS_HGS2_BASE`；
- B：`STD_L06_PID_AS_HGS2_ORACLE_K40`；
- C：`STD_L06_PID_AS_HGS2_RLS_SHADOW`。

正式预算最多6次，不追加。A/B仍按两对评价预注册性能门槛；C只评价估计、直接补偿为零和倍率为1，不参与A/B性能优劣判定。
