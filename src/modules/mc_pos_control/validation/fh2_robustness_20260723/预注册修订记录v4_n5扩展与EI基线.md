# FHv2 预注册修订记录 v4（2026-07-23，矩阵飞行前）

经用户批准（时间预算内），矩阵计划做两处扩展，其余不变：

1. **关键点扩到 n=5**：L06 = FH2_06(v2 门槛) + 矩阵 4 次；L08 = FH2_08(门槛) + 矩阵 4 次；MM070 = 矩阵 5 次。判定规则不变（均值过门且单次过门率 ≥2/3）。
2. **新增 EI 文献基线**：经典三脉冲 Extra-Insensitive 整形（Singhose 等；权重 ((1+K)/4, (1-K)/2, (1+K)/4)，间隔与 ZVD 同阻尼半周期），工况 `EI_L06`×3、`EI_MM070`×3，协调器与 FHv2 相同（模式6），只隔离整形器差异。EI 为对照组，预期其任务时序偏差/失配性能弱于 FHv2，不参与 matrix_pass 总判定。

总预算：L05×3 + L06×4 + L08×4 + P75×3 + MM070×5 + EI_L06×3 + EI_MM070×3 + BASE_L08×3 = **28 次**，预计约 115 分钟。单次无效处理规则不变（记录 n<3，最多补 2 次）。
